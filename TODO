* Parse current directory tree into memory - single list of all files
* Apply rules to list of files - need wildcard handling code to work
** test directory character one at a time from the pattern : * - means any char up to a /, ** - means any character to end of string (true)
** split file-entry into directory & file & extension
** test file & extension first : * means any character

